package model;

public class GameUpdate
{
	private boolean initialUpdate;
	
	//public GameUpdate()
	//This needs to store probabilities for each match odds runner that gets pushed
	//It also needs the timestamp for it
	//It also needs the real time the last update was
	
	//Special case needs home team name
	//Special case needs away team name
	//Special case needs to tell favourite of both teams
	//Special case needs game start time
}
